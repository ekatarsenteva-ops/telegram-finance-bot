# Саммари проекта: Telegram-бот учёта финансов

## Что это

Telegram-бот для учёта доходов/расходов двух объектов — «Школа языков» и
«Недвижимость». Пользователь пишет операцию свободным текстом
(«Пришло 15000 от Пети», «Купили материал за 6000, зачет в аренду Ивану»),
бот распознаёт её через Google Gemini, уточняет недостающие поля через диалог
с кнопками, и сохраняет в базу данных Postgres (Neon).

Ключевые возможности:
- Свободный текстовый ввод операций (доход / расход / плановый платёж)
- Диалог-уточнение недостающих полей (объект, контрагент, категория, сумма, дата)
- Групповые расходы с разделением долга между несколькими людьми
  ("Пикник 9000, делим поровну на Иру, Марию, Петра")
- Погашение долгов свободной фразой ("Ира вернула долг") — с нечётким
  сопоставлением имён (уменьшительные формы/опечатки через Gemini)
- Взаимозачёты (offset) — расход "в счёт" будущего платежа
- Команды: `/balance`, `/debts`, `/export` (Excel-отчёт), `/settings`, `/cancel`
- Ограничение доступа — бот отвечает только владельцу + списку доверенных
  Telegram ID
- Веб-дашборд аналитики (Streamlit) — метрики прихода/расхода/баланса по
  объектам, динамика по месяцам, разбивка расходов по категориям, открытые
  долги и взаимозачёты; защищён паролем

## Технологии

- **aiogram 3.15** — Telegram Bot framework (FSM-диалоги)
- **Google Gemini** (`gemini-flash-latest`) — распознавание естественного
  языка, структурированный вывод в JSON
- **PostgreSQL** на **Neon** (serverless) — хранение данных
- **psycopg** (async pool) — доступ к БД
- **openpyxl** (предположительно, через `services/reporting.py`) — экспорт в
  Excel

## Структура репозитория

```
telegram-finance-bot/
├── main.py                     — точка входа, регистрация роутеров/middleware
├── config.py                   — настройки из .env (токены, доступ)
├── bot/
│   ├── middlewares.py          — контроль доступа по telegram_id
│   ├── states.py               — состояния FSM-диалога
│   └── routers/
│       ├── dialog.py           — основная логика диалога-уточнения
│       ├── callbacks.py        — обработка нажатий на inline-кнопки
│       ├── commands.py         — /start, /help, /cancel, /settings
│       └── reporting.py        — /balance, /debts, /export
├── db/
│   ├── connection.py           — пул подключений к Postgres
│   └── queries.py              — все SQL-запросы
├── services/
│   ├── claude_parser.py        — вызовы Gemini (парсинг + fuzzy-match имён)
│   ├── expense_splits.py       — логика группового расхода/долгов
│   ├── offset_manager.py       — логика взаимозачётов
│   └── reporting.py            — сборка Excel-отчёта
├── models/schemas.py           — Pydantic-схемы распознанной операции
├── prompts/parse_system_prompt.py — системный промпт для Gemini
├── migrations/                 — SQL-схема + сид категорий
└── dashboard/                  — веб-дашборд аналитики (Streamlit, отдельный деплой)
    ├── app.py                  — точка входа, фильтры, вызывает секции
    ├── auth.py                 — парольный гейт
    ├── db.py                   — синхронное подключение к Postgres
    ├── queries.py              — SQL-запросы для отчётов (pandas.DataFrame)
    ├── sections.py             — отрисовка секций (метрики/графики/таблицы)
    └── requirements.txt        — зависимости дашборда (отдельно от бота)
```

## Текущее состояние (по состоянию на 2026-08-07)

### Готово и проверено вручную
- **US-1** — свободный приход ✅
- **US-3** — групповой расход + погашение долга ✅ (включая фиксы ниже)
- **US-4** — `/balance`, `/debts` ✅
- **US-6** — категории кнопками ✅ (замечено попутно)
- Ограничение доступа (owner + allowlist) добавлено и работает ✅
- Дашборд аналитики на Streamlit — реализован, задеплоен на Streamlit
  Community Cloud, код опубликован на GitHub (`ekatarsenteva-ops/telegram-finance-bot`) ✅

### Не доделано / не проверено
- **US-2** — входящий платёж с зачётом (offset) — начат, не завершён
  (упёрлись в дневную квоту Gemini)
- **US-5** — экспорт в Excel (`/export`) — не тестировали
- Доп. сценарии: `/cancel` посреди диалога, некорректная дата в `/export`,
  проверка `raw_ai_log` в БД — не тестировали
- Кнопка открытия дашборда прямо из бота (`WebAppInfo`) — не добавлена,
  ждёт публичного URL после деплоя на Streamlit Cloud
- Распознавание речи (голосовой ввод) — отложено пользователем на обдумывание

### Известные ограничения
- **Дневная квота Gemini на бесплатном тарифе — 20 запросов/день.** Легко
  исчерпывается во время активного тестирования (каждое сообщение с новой
  операцией + иногда доп. запрос на нечёткое сопоставление имени при
  погашении долга). При превышении лимита бот отвечает "Не удалось
  разобрать операцию" — это не баг, а исчерпанная квота (сбрасывается раз
  в сутки).
- Секретные ключи (Telegram-токен, Anthropic/Gemini API-ключи, строка
  подключения к БД) лежат в `.env`, который **не в git** — но были
  показаны в переписке в открытом виде. После завершения тестирования их
  стоит ротировать (см. README, раздел "После первого успешного запуска").

## Найденные и исправленные баги в этой сессии

1. **Контрагент проглатывал весь свободный текст.** При уточнении поля
   "контрагент" бот принимал любой ответ буквально, включая целые
   предложения. Исправлено: добавлена кнопка "Без контрагента" + валидация
   (отклоняет фразы длиннее 4 слов или содержащие цифры, просит переформулировать).

2. **Погашение долга не работало при опечатках/маленькой букве/другой форме
   имени** ("петя" вместо "Петя вернул", "Петя" вместо "Петр"). Исправлено:
   сначала ищем точное совпадение с именами из открытых долгов в базе
   (регистронезависимо), если не находим — просим Gemini сопоставить
   уменьшительную форму/опечатку с реальным именем должника.

3. **У бота не было вообще никакой проверки доступа** — любой человек в
   Telegram мог найти бота и писать операции, которые попадали в базу.
   Исправлено: `AccessControlMiddleware` — тихо отбрасывает все сообщения
   от пользователей вне списка `owner_telegram_id` + `allowed_telegram_ids`.

4. **В экспорте Excel колонка "Статус" показывала "платёж" для плановых
   платежей вместо приход/расход.** Исправлено в `services/reporting.py`:
   "Статус" теперь всегда одна из трёх категорий (приход/расход/взаимозачёт),
   а более детальный лейбл ("платёж") остался только в колонке "Тип".

5. **Дубли категорий из-за регистра** ("Уборка" и "уборка" — две разные
   строки в базе из-за case-sensitive уникального ограничения). Исправлено
   в `db/queries.py`: `get_or_create_category`/`get_or_create_counterparty`
   теперь ищут регистронезависимо (`lower(name) = lower(%s)`). Существующий
   дубль в базе объединён вручную.

## Git-история

Основной код бота живёт в `/home/coder/workspace` (ветка `master`, там же
есть ещё один, не связанный с ботом, проект). Для публикации используется
отдельная чистая ветка `finance-bot-only`, собираемая через
`git subtree split --prefix=telegram-finance-bot`, и пушится в отдельный
GitHub-репозиторий `ekatarsenteva-ops/telegram-finance-bot` (ветка `main`).

Если появятся новые правки в `telegram-finance-bot/`, чтобы опубликовать их:
```
git branch -D finance-bot-only
git subtree split --prefix=telegram-finance-bot -b finance-bot-only
git push origin finance-bot-only:main
```
